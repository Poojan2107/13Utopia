# Team portraits

Drop approved portraits here:

```
public/team/[id].webp
```

IDs match `src/data/people.ts` (`studio-lead`, `creative-director`, …).

Then set `portrait: "/team/[id].webp"` on that person and redeploy.
